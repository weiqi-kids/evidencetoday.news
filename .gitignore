# build output
dist/

# node
node_modules/

# astro
.astro/

# env
.env
.env.*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
*.swp
*.swo

# logs
*.log

# database
*.db

# Local-only binary asset (upload via deployment CMS or manual server upload)
public/images/authors/luo-yang.jpg

# YouTube sync output — generated at build time by scripts/sync-youtube-shorts.mjs.
# Not tracked because the runner regenerates it on every deploy; checking it in
# would create perpetual diff noise and mask sync failures behind stale data.
src/data/youtube-shorts.json

# Claude Code personal overrides (per-contributor; .claude/settings.json is shared and tracked)
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# Generated OG images (build-time output)
public/og/**/*.png

# Generated stock-image dedup list (prebuild output for editor「AI 找圖庫」)
public/admin/used-images.json

# Generated OG preview images (local verification output)
public/og-preview/*.png
public/og-preview/**/*.png
# Local OG preview workspace (HTML + derived image thumbnails)
public/og-preview/
# Generated OG media artifacts — keep PRs text-only
public/og/**/*.jpg
public/og/**/*.jpeg
public/og/**/*.webp
public/og/**/*.gif
public/og/**/*.pdf
public/og/**/*.zip

# Audience insights — 私密經營數據，不 commit、不 deploy
data/audience-insights.json
data/seo-daily/

# Topic backlog 本地層 — GSC 原始查詢字串與曝光／排名／需求分，不 commit、不 deploy。
# 公開層 data/topic-backlog.json 照常追蹤，兩層以 id 對應（見 docs/playbooks/topic-backlog.md）。
data/topic-backlog.local.json
