# 到期提醒

**每次 session 啟動時掃這份**：今天 ≥ 到期日的項目要主動提醒使用者；未到期的不要提（避免噪音）。

規則：
- 這裡只放「到了某一天要做某件事」，**不放判斷用的數字基準**——基準會過期，到期時一律現場跑指令取得當下數據。
- 做完就把該列移到「已結案」並註明結論落在哪份文件，不要刪掉（保留決策軌跡）。
- 日期一律台灣時間（UTC+8）。

---

## 待辦

| 到期日 | 事項 | 怎麼做 |
|---|---|---|
| 2026-08-21 | **驗證成分頁重寫有沒有換到索引回補** | 跑 `pnpm index:coverage`，看 2026-08-06 重寫的 22 篇成分頁是否從 `Discovered - currently not indexed` 轉為已索引。**這是那批工作唯一的驗收機制**：假設是「跨頁樣板重複是未索引的病因」，成立才算證明；若索引狀態沒動，表示樣板不是主因，要回頭重查（查法見 `playbooks/analytics.md`）。判讀時記得排除當時仍在排程中的稿。 |
| 2026-08-15 | news 發文頻率**期中**檢查 | 照 `playbooks/news-cadence-review.md` 走完整流程。**這是期中檢查不是結案**：當時該批排程稿尚未全部上線，最晚上線的幾篇 Google 還沒爬完，數據必然不足。期中只看趨勢方向，方向明確才動結構；完整結論留到該批全發完後再下。 |

## 已結案

（尚無）

---

## 背景：news 發文頻率這條為什麼存在

使用者於 2026-08-05 決定**發文維持原節奏、news 不減量**，理由是趨勢新聞有時效性、壓著不發等於作廢，且當時網站處於成長期，不宜在高峰期亂動結構。同時撤回前一天規劃的 news 降頻（評分門檻已還原、主機 cron 始終是每日）。

2026-08-06 使用者把複查日從原訂的約 09-01 提前到 08-15，並要求作為期中檢查。
