# 到期提醒

**每次 session 啟動時掃這份**：今天 ≥ 到期日的項目要主動提醒使用者；未到期的不要提（避免噪音）。

規則：
- 這裡只放「到了某一天要做某件事」，**不放判斷用的數字基準**——基準會過期，到期時一律現場跑指令取得當下數據。
- 做完就把該列移到「已結案」並註明結論落在哪份文件，不要刪掉（保留決策軌跡）。
- 日期一律台灣時間（UTC+8）。

---

## 待辦

> ⚠️ **這兩項都必須在「有 Google 資料存取憑證」的環境跑**（營運主機，或依
> `setup-google-data-access.md`「丁段」設好金鑰並開新 session 的機器）。開發用的
> Windows 本機沒有憑證，`pnpm perf` / `index:coverage` 會直接失敗。
> 兩項的 playbook 都明文禁止拿文件裡的舊數字代替現場數據——沒有憑證就不要硬做，
> 那會做出相反的決定。

| 到期日 | 事項 | 怎麼做 |
|---|---|---|
| 2026-08-21 | **驗證成分頁重寫有沒有換到索引回補** | 跑 `pnpm index:coverage`，看 2026-08-06 重寫的 22 篇成分頁是否從 `Discovered - currently not indexed` 轉為已索引。**這是那批工作唯一的驗收機制**：假設是「跨頁樣板重複是未索引的病因」，成立才算證明；若索引狀態沒動，表示樣板不是主因，要回頭重查（查法見 `playbooks/analytics.md`）。判讀時記得排除當時仍在排程中的稿。**另注意**：排程於 08-18～08-21 上線的 4 篇舊成分頁（鋸棕櫚、銀杏、左旋肉鹼、蔓越莓）**不屬於**那 22 篇重寫批次，判讀時不要混入。 |
| 2026-08-21<br>（原訂 08-15，順延） | news 發文頻率**期中**檢查 | 照 `playbooks/news-cadence-review.md` 走完整流程。**這是期中檢查不是結案**：當時該批排程稿尚未全部上線，最晚上線的幾篇 Google 還沒爬完，數據必然不足。期中只看趨勢方向，方向明確才動結構；完整結論留到該批全發完後再下。<br>**順延原因**（2026-08-17 使用者裁示）：原訂 08-15 到期時，手邊環境無 Google 憑證無法取得當下數據；與其用舊數字硬做，不如與 08-21 的索引驗收併在同一次、同一個有憑證的環境一起跑。順延也讓樣本多累積幾天，對「期中看方向」這個目的有利無害。 |

## 已結案

（尚無）

---

## 背景：news 發文頻率這條為什麼存在

使用者於 2026-08-05 決定**發文維持原節奏、news 不減量**，理由是趨勢新聞有時效性、壓著不發等於作廢，且當時網站處於成長期，不宜在高峰期亂動結構。同時撤回前一天規劃的 news 降頻（評分門檻已還原、主機 cron 始終是每日）。

2026-08-06 使用者把複查日從原訂的約 09-01 提前到 08-15，並要求作為期中檢查。

## 待補：69 篇文章的 coverAlt（無到期日，遇到能看圖的環境就做）

`pnpm check:site` 每次都會提醒目前的缺口數字（不要把數字寫進這份文件）。

**為什麼還沒做**：封面多半是 Pexels／Unsplash 外連，**遠端** agent 環境對外連圖床是
`403 connect_rejected`（組織網路政策，不可繞過）。alt 要寫得對，得先看得到那張圖。

**但這不是全環境都做不到（2026-08-17 更正）**：Windows 本機（`e:\Noah\ET`）實測
`images.unsplash.com` / `images.pexels.com` 皆回 200，`curl` 下載後用 Read 工具可直接看圖，
當天已用這個方式替 20 篇新稿逐張看圖寫完 `coverAlt`。所以這條的正確描述是
「遠端 agent 做不到、本機做得到」，不是「沒有環境做得到」——要清這 69 篇的欠帳，
在本機開 session 跑即可，做法見 `playbooks/editor-images.md`。

**為什麼不把它做成擋 build 的規則**：必填但不檢查正確性，結果就是大家隨便填一個值讓 gate
過——2026-08-07 的闢謠封面圖事件（74 篇填了不相干的圖）正是這樣來的。
見 `docs/pitfalls.md`「要求有值而不要求值正確，比沒有要求更糟」。

**做的時候要注意**：Article.astro 在缺 `coverAlt` 時退回用文章標題當 alt。那不是圖片描述，
是標題——螢幕閱讀器聽到的等於沒有說明圖裡有什麼。所以 gate 把「alt 等於 h1」也算缺漏。

**順帶要判斷的**：補 alt 的同時就能檢查圖文是否相符。本輪用文字比對做過一次粗篩，
結論是那個方法不可用——中文的「白內障」與「視力度數」沒有共同字元，字元重疊率會把
正確的配圖判成不相關。抽查有 alt 的幾篇（膽鹼→雞蛋、膠原蛋白→大骨湯、白內障→驗光試鏡架）
配圖都切題。圖文相關性只能靠人看，不要再嘗試用文字相似度自動判斷。

## 2026-08-25 之後：補上睡眠軟糖的內鏈

`articles/thailand-sleep-gummies-bring-back-taiwan` 排程 2026-08-25 發布。上線後，
把 `import-melatonin-taiwan-customs` 的「買的是軟糖、飲品，還是藥錠？」一節末段那句
「那件事另有一篇專門講」改成指向它的連結。

**現在不能加連結**：目標頁還沒發布，`dist` 裡不存在，會變成死連結，`pnpm check:schedule`
會擋。這是排程稿與已發布稿之間的固有時間差，不是 bug。

## 待人執行（agent 做不到，別再指派給 agent）

| 項目 | 為什麼只能人做 |
|---|---|
| GEO 的 D2 人工抽測（10 題問各家 AI） | 要開瀏覽器逐題問 ChatGPT／Perplexity／Gemini 並判讀引用位置。CCR 環境對外網 `403 connect_rejected`，也沒有可驅動的瀏覽器。指派給 agent 只會得到編造的結果。 |
| ~~Bing Webmaster Tools 開通與提交~~ | **這一項是我寫錯的，2026-08-09 更正。** Bing 早在 2026-06-17 就完成帳號、驗證與 sitemap 提交（`geo-offsite.md` 三個框都是 `[x]`），且 `public/<key>.txt` + `scripts/indexnow-submit.mjs` + CI 的 indexnow job 讓每次部署都主動推送。我看到章節標題寫「最高槓桿」就當成待辦，沒往下看那三個打勾的框。**真正該查的是「已經接好卻只帶來 7 個階段」的原因**，不是「去把它接上」。 |
| 69 篇文章的 coverAlt | 要看得到圖才寫得對，外連圖床同樣 403。詳見上方。 |
| 短影音頁要走哪條路 | 每支影片真正的獨有內容只有約 200 字，不是「有內容沒渲染」。要嘛接受不被單獨索引、定位成 YouTube 落地頁，要嘛改成一主題一頁收數支影片（URL 要改、要轉址）。這是產品決策不是技術決策，見 `docs/page-rules/videos.md`。 |
